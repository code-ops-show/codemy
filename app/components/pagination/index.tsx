import * as React from 'react'
import { FunctionComponent } from 'react'

type PaginationType = {
  current_page: number,
  total_pages: number
}

const Pagination: FunctionComponent<PaginationType> = (props) => {
  return (
    <div>
      
    </div>
  )
}